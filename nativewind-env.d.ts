/// <reference types="nativewind/types" />
// This file tells TypeScript that React Native components (View, Text, etc.)
// accept a "className" prop — which NativeWind adds at build time.
// Without this file, TypeScript would show errors like:
//   "Property 'className' does not exist on type ViewProps"
